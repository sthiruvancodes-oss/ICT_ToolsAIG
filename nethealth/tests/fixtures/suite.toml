name = "fixture-suite"
timeout_seconds = 1.5
warn_tls_days = 21

[[checks]]
name = "loopback"
type = "icmp"
host = "127.0.0.1"

[[checks]]
name = "dns-local"
type = "dns"
query = "localhost"
record = "A"
expect = ["127.0.0.1"]
